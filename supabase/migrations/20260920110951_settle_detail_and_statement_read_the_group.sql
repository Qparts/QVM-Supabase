-- settle_detail_and_statement_read_the_group
--
-- Applied directly to QVM/dev on 2026-09-20 11:09 (recorded in supabase_migrations.schema_migrations
-- as 20260920110951) without a file in the repository. This placeholder exists so the branch runner,
-- which refuses to run while a recorded version has no file, can proceed. The runner never executes
-- it: the version is already in the history. When the author commits the real SQL, it should
-- replace this file under this same version number.
SELECT 1;
